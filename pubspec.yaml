name: study_progress
description: 学习进度 - 本地记录网课学习进度工具
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.4.0
  path: ^1.9.0
  path_provider: ^2.1.0
  intl: ^0.20.2
  file_picker: ^8.0.0
  permission_handler: ^12.0.0
  share_plus: ^10.0.0
  device_info_plus: ^11.0.0
  shared_preferences: ^2.3.0
  provider: ^6.1.0
  fl_chart: ^0.69.0
  http: ^1.2.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
